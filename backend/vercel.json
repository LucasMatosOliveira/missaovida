{
    "version": 2,
    "builds": [
      {
        "src": "routes/authRoutes.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "routes/auth/(.*)",
        "dest": "routes/authRoutes.js"
        }
    ]
}
