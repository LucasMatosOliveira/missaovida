{
    "version": 2,
    "builds": [
      {
        "src": "routes/authRoutes.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "routes/(.*)",
        "dest": "routes/authRoutes.js"
        }
    ]
}
